# End-to-End Test — Round 1 (2026-08-05, Mac dev machine, synthetic data)

Scope: the real operator chain exercised from the CLI entry points the UI shells
out to, on synthetic data (per standing practice: real field names, fake numbers).

## What ran

1. **Synthetic ODD build** — 20-account frame from the shared test fixture
   (`tests/_fixtures.synthetic_rewards`), saved as
   `9999-2026-06-Synthetic Test CU-ODD.xlsx` with a matching one-client config.
2. **Full ARS run** — `01_Analysis/run.py <odd> --month 2026.06 --client 9999
   --output-dir <tmp> --config <tmp> --template <repo template>`:
   26/26 modules ran, 75 slides generated, 57-slide deck built, Excel +
   run-report emitted, integrated deck QA executed. Exit 0.
3. **TXN chain** — covered by `test_full_deck_integration` (all TXN sections over
   synthetic combined_df → real deck build → 39+ slides). The TXN *file
   discovery* path (TXN Files/{CSM}/{client} layout, parquet cache) is not
   exercisable on this machine.
4. **Formatting** — covered by the 32-test `00_Formatting` suite (month
   resolver, empty/banner ODD handling, trans gather/zip, v3 staging).

## Findings

| # | Severity | Finding | Status |
|---|----------|---------|--------|
| 1 | MAJOR | `run.py --output-dir` ended successful runs with an ERROR banner: delivery step copied the deck onto itself (`SameFileError` escaped the `PermissionError`-only retry). | **Fixed** on main |
| 2 | MINOR | Deck QA (correctly) FAILs the synthetic deck: slides "ARS Mailer Revisit – Swipes/Spend" and "Program Responses to Date" ship title-only when their data gates off. Open question: should chart-less slides be dropped from the deck instead of shipping as title-only bodies? | Open — fed to persona round |
| 3 | INFO | Test suite litter (root PNGs, `competition_diagnostic.txt`, `Actionable Lists/`) traced to dead `ctx.paths` fallbacks — cross-sell lists were landing in the CWD in production too. | **Fixed** on main |

## Windows-only verification still pending (work PC)

- TXN full run with real transaction files (parquet cache, eligible-line log per #216 fix)
- generate-from-path on a real UNC ODD (#231)
- duckdb wheel present in the CSM python bundle; parity goldens on two reference
  clients with `ARS_PARITY_CAPTURE=1` (v3 Wave 1 prerequisites)
