# Recommended SLIDE_MANIFEST defaults — ARS Reg E section
#
# Cycle 3 of the section-by-section triage pass (deck order). 12 emitted
# slides (A8.4a was already eliminated in code -- its run() result is
# discarded; it only feeds ctx.results for A8.4b/c).
#
# Review notes (2026-06-11):
#   Main-deck story (3 slides): opt-in status donuts (A8.1) -> L12M
#   momentum (A8.3) -> branch variation (A8.4b).
#   Chart fixes this cycle: ~23 hardcoded off-brand colors across the
#   section (navy #1B2A4A x9, blues #B0C4DE/#4A6FA5/#1B4F72, red
#   #E63946, string "red" reference lines x7) moved onto the brand
#   palette; a dead `_DARK if False` ternary removed; A8.4b's subtitle
#   no longer names the line color; A8.12's trend fit was navy-on-navy
#   (invisible against the data line) -- now accent. The A8.10/A8.11
#   funnels were rebuilt onto shared draw_funnel (was a custom
#   FancyBboxPatch render with a rainbow palette ending in purple), so
#   they now match the A3 eligibility funnel language.
#   Reference-line convention going forward: overall-average / portfolio
#   lines are brand-accent dashed (matches the DCTR cycle).
#   Denominators: clean -- everything is computed on Eligible Personal
#   per the W1 denominator-law audit.

section: rege
sheet: ARS - RegE
defaults:
  A8.1: Y      # all-time vs L12M opt-in donuts (headline)
  A8.3: Y      # L12M monthly trend + volume context
  A8.4b: Y     # branch volumes + opt-in rate + portfolio avg
  A8.2: A      # by year / by decade context
  A8.4c: A     # branch volume-vs-rate scatter
  A8.5: A      # by account age
  A8.6: A      # by holder age (historical vs L12M)
  A8.7: A      # by product code (top 15)
  A8.10: A     # all-time funnel (now draw_funnel)
  A8.11: A     # L12M funnel (now draw_funnel)
  A8.12: A     # 24-month trend + slope fit
  A8.13: A     # branch x month pivot (Excel)
